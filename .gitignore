# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node / Expo
node_modules/
.expo/
dist/
web-build/

# Env / secrets
.env
.env.local
*.env
!.env.example
# Firebase client config (MP-068) — gitleaks flags its API key (gcp-api-key rule); stored via EAS's
# file-type environment variables instead (see docs/MP-068-070-push-notifications.md).
mobile/google-services.json

# OS
.DS_Store
Thumbs.db
