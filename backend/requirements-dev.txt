-r requirements.txt
ruff>=0.16.4,<1
mypy>=2.3.1,<3
pip-audit>=2.10.1,<3
# supabase/seed/load_master_catalogue.py's dependency — dev-only tooling, not shipped by the app
# itself, hence requirements-dev.txt rather than requirements.txt. Was previously undeclared
# entirely (PR #10 review finding).
openpyxl>=3.1.5,<4
